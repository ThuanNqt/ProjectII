header.header
  .container-fluid
    .row.align-items-center
      .col-3
        .inner-logo
          a(href="/admin/dashboard") ADMIN
      .col-9
        .dropdown.text-right
          a.dropdown-toggle.d-inline-block(href="#" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false")
            img(src=`${user.avatar}` alt="avatar" style="width: 30px; height: 30px; border-radius: 50%;")
          .dropdown-menu.dropdown-menu-right
            a.dropdown-item(href=`/admin/accounts/detail/${user._id}`) Thông tin cá nhân
            .dropdown-divider
            a.dropdown-item(href="/admin/auth/logout") Đăng xuất